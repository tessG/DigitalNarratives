export type TagFrequencyItem = { tag: string; count: number }
export type CooccurrenceItem = { tagA: string; tagB: string; count: number }
export type TemporalItem = { date: string; tag: string; count: number }
export type TensionPost = { content: string; date: string; platform: string }
